# Dependencies are managed in pyproject.toml.
# Install only what you need:
#
#   pip install -e ".[ollama,duckduckgo]"     # zero-key default (Ollama + DuckDuckGo)
#   pip install -e ".[ollama,duckduckgo,ui]"  # + Streamlit UI
#   pip install -e ".[claude,tavily]"         # Claude + Tavily
#   pip install -e ".[openai,duckduckgo]"     # OpenAI + DuckDuckGo
#   pip install -e ".[all]"                   # everything
